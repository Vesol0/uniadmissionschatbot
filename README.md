**INSTALLATION**

``pip install -r requirements.txt`` or ``pip3 install -r requirements.txt``

*** 

To run the app, make sure you are in the project directory using 'cd', then enter in your terminal ``streamlit run streamlit_app.py``


